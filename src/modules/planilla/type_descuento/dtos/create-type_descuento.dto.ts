export interface ICreateTypeDescuentoDto {
  description?: string,
  isEdit?: boolean,
  is_escalafon?: boolean,
  judicial?: boolean,
  key?: string,
  except?: boolean,
  plame?: boolean,
  renta?: boolean,
  state?: boolean
}